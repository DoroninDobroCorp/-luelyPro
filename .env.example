# ============================================================================
# CluelyPro - Пример конфигурации
# ============================================================================
# БЫСТРЫЙ СТАРТ:
# 1. Скопировать: cp .env.example .env
# 2. Заполнить GEMINI_API_KEY в .env
# 3. Запустить: ./run.sh

# ============================================================================
# 🔑 API Keys
# ============================================================================
# Gemini (ОБЯЗАТЕЛЬНО): https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# OpenAI (опционально, fallback если Gemini упал)
OPENAI_API_KEY=your_openai_key_here

# ============================================================================
# 🎤 ASR (Распознавание речи)
# ============================================================================

# Модель faster-whisper:
# - tiny:           быстро (~0.5с латентность)
# - small:          баланс (~1-2с латентность) - рекомендуется
# - medium:         качественно (~2-3с)
# - large-v3-turbo: максимальное качество (~3-5с)
ASR_MODEL=small

# Язык распознавания
# ru = русский, en = английский
ASR_LANG=ru

# Устройство для ASR (опционально)
# cuda = GPU, cpu = процессор, auto (по умолчанию) = автовыбор
# Если ASR тормозит или ошибки CUDA - раскомментируйте:
# ASR_DEVICE=cpu

# ============================================================================
# 🎙️ VAD (Детектор речи) - для продвинутых
# ============================================================================

# Бэкенд VAD (Voice Activity Detection)
# webrtc = быстрый, но менее точный (по умолчанию)
# silero = нейросеть, более точный, чуть медленнее
# Рекомендация: оставить webrtc, менять только если есть проблемы
# VAD_BACKEND=webrtc

# ============================================================================
# 📝 Тезисы
# ============================================================================

# Интервал повтора тезиса (секунды)
# Система повторяет тезис, пока вы его не озвучите
# 
# Рекомендации:
# - 3-5 сек:   быстрый повтор (для экзаменов)
# - 10-15 сек: комфортный темп (для тренировки)
THESIS_REPEAT_SEC=10

# ============================================================================
# 📊 Логирование (для отладки)
# ============================================================================

# Уровни логирования:
# - DEBUG:   все подробности (для поиска проблем)
# - INFO:    основные события (рекомендуется)
# - WARNING: только предупреждения
# - ERROR:   только ошибки

# Консольный вывод (что видно в терминале)
# CONSOLE_LOG_LEVEL=INFO

# Файловый лог (что пишется в logs/assistant.log)
# FILE_LOG_LEVEL=DEBUG

# ============================================================================
# 💡 Полезные комбинации
# ============================================================================
#
# 🎓 Для экзамена (быстрые подсказки):
#   THESIS_REPEAT_SEC=3
#   ASR_MODEL=tiny
#
# 🏋️ Для тренировки (комфортный темп):
#   THESIS_REPEAT_SEC=15
#   ASR_MODEL=small
#
# 🧪 Быстрый тест (10 секунд):
#   RUN_SECONDS=10 ASR_MODEL=tiny ./run.sh
#
# 🐛 Отладка (подробные логи):
#   FILE_LOG_LEVEL=DEBUG CONSOLE_LOG_LEVEL=DEBUG ./run.sh
#
